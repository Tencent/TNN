include_directories(${CMAKE_CURRENT_SOURCE_DIR}/source/tnn/device/opencl)
include_directories(${CMAKE_CURRENT_SOURCE_DIR}/third_party/opencl/include)
include_directories(${CMAKE_CURRENT_SOURCE_DIR}/third_party/stb)
include_directories(${CMAKE_CURRENT_SOURCE_DIR}/platforms/android/src)

set(COMMON_SRC ${CMAKE_CURRENT_SOURCE_DIR}/platforms/android/src/test_common.cc)

if (TNN_OPENVINO_ENABLE)
    add_library(inference_engine STATIC IMPORTED)
    add_library(inference_engine_legacy STATIC IMPORTED)
    add_library(inference_engine_transformations STATIC IMPORTED)
    add_library(inference_engine_lp_transformations STATIC IMPORTED)
    add_library(MKLDNNPlugin STATIC IMPORTED)
    add_library(ngraph STATIC IMPORTED)
    add_library(pugixml STATIC IMPORTED)
    set_target_properties(inference_engine PROPERTIES IMPORTED_LOCATION ${CMAKE_CURRENT_SOURCE_DIR}/source/tnn/network/openvino/thirdparty/openvino/lib/${LIB_PFX}inference_engine.lib)
    set_target_properties(inference_engine_legacy PROPERTIES IMPORTED_LOCATION ${CMAKE_CURRENT_SOURCE_DIR}/source/tnn/network/openvino/thirdparty/openvino/lib/${LIB_PFX}inference_engine_legacy.lib)
    set_target_properties(inference_engine_transformations PROPERTIES IMPORTED_LOCATION ${CMAKE_CURRENT_SOURCE_DIR}/source/tnn/network/openvino/thirdparty/openvino/lib/${LIB_PFX}inference_engine_transformations.lib)
    set_target_properties(inference_engine_lp_transformations PROPERTIES IMPORTED_LOCATION ${CMAKE_CURRENT_SOURCE_DIR}/source/tnn/network/openvino/thirdparty/openvino/lib/${LIB_PFX}inference_engine_lp_transformations.lib)
    set_target_properties(MKLDNNPlugin PROPERTIES IMPORTED_LOCATION ${CMAKE_CURRENT_SOURCE_DIR}/source/tnn/network/openvino/thirdparty/openvino/lib/${LIB_PFX}MKLDNNPlugin.lib)
    set_target_properties(ngraph PROPERTIES IMPORTED_LOCATION ${CMAKE_CURRENT_SOURCE_DIR}/source/tnn/network/openvino/thirdparty/openvino/lib/${LIB_PFX}ngraph.lib)
    set_target_properties(pugixml PROPERTIES IMPORTED_LOCATION ${CMAKE_CURRENT_SOURCE_DIR}/source/tnn/network/openvino/thirdparty/openvino/lib/${LIB_PFX}pugixml.lib)
    target_link_libraries(TNN inference_engine)
    target_link_libraries(TNN inference_engine_legacy)
    target_link_libraries(TNN inference_engine_transformations)
    target_link_libraries(TNN inference_engine_lp_transformations)
    target_link_libraries(TNN MKLDNNPlugin)
    target_link_libraries(TNN ngraph)
    target_link_libraries(TNN pugixml)
endif()