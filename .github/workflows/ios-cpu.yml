name: ios-cpu
on:
  push:
    branches:
      - enhance-ci
  pull_request:
    branches:
      - enhance-ci
jobs:
  ios-iphone-os:
    runs-on: macos-latest
    steps:
    - uses: actions/checkout@v2
    - name: build
      run: ./scripts/build_ios.sh
