name: ios-cpu
on:
  push:
    branches:
      - add-github-action
  pull_request:
    branches:
      - add-github-action
jobs:
  ios-iphone-os:
    runs-on: macos-latest
    steps:
    - uses: actions/checkout@v2
    - name: build
      run: ./scripts/build_ios.sh
